// Vue.prototype.plugins.$log is a better approach to avoid confusion that it is coming
// from a plugin and not a Vue instance.

// Look into the automatic installation code.
/*
if (typeof window !== 'undefined' &&....)
*/
